{
  "name": "@driftcore/agent-runner",
  "version": "0.1.0",
  "description": "Reference agent runner for DriftCore",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --pretty --noEmit",
    "test": "npm run build && node --test dist/__tests__",
    "integration": "npm run build && node dist/integration/smoke.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@types/node": "^20.11.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
