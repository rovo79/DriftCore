# @driftcore/agent-runner (placeholder)

This directory intentionally provides a minimal placeholder package so CI jobs that still run `npm install --prefix packages/agent-runner` do not fail.

The runtime implementation of an agent runner is not currently present in this repository.
